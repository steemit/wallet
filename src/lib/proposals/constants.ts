/** Steem DAO treasury account (legacy `REFUND_ACCOUNTS`). */
export const DAO_TREASURY_ACCOUNT = 'steem.dao';

/** Accounts whose proposals are labeled as burn (legacy `BURN_ACCOUNTS`). */
export const PROPOSAL_BURN_ACCOUNTS = ['null'];
